import 'package:flutter/widgets.dart';

class ScheduleChanges extends StatefulWidget {
  const ScheduleChanges({super.key});

  @override
  State<ScheduleChanges> createState() => _ScheduleChangesState();
}

class _ScheduleChangesState extends State<ScheduleChanges> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}